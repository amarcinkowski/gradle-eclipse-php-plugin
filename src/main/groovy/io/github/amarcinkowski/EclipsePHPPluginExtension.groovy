package io.github.amarcinkowski

class EclipsePHPPluginExtension {
	/**
	 * Used in Eclipse PHP 
	 * 
	 * exclude folders from validation e.g. 'vendor' folder,
	 * which is used as library folder rather than source folder 
	 */
	String libraryFolders
}
